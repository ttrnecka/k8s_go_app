---
apiVersion: v1
kind: Service
metadata:
  name: myapp
  namespace: app
spec:
  selector:
    app: myapp
    version: blue  # Switch to 'green' for blue-green deployment
  ports:
    - port: 80
      targetPort: 8080
      name: http